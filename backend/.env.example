PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=child_vaccination_db
JWT_SECRET=replace_with_a_long_random_secret

# Optional: chatbot works from verified MySQL data without this key.
# Add a server-side OpenAI API key to improve wording for FAQ responses.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
